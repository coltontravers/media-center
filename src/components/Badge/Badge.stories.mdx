import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { text, color, select } from "@storybook/addon-knobs";
import Badge from "./Badge";
import Button from "../Button/Button";
import { badgeThemes, badgePositions } from "../../constants/badge";

<Meta title="Badge" component={Badge} />

<Preview>
    <Story name="inside button">
        <div style={{ margin: "1.5em" }}>
            <Button>
                Movies
                <Badge
                    label={text("label", "30")}
                    backgroundColor={color("background color")}
                    textColor={color("text color")}
                    theme={select(
                        "theme",
                        Object.keys(badgeThemes),
                        "info",
                        ""
                    )}
                    position={select(
                        "position",
                        Object.keys(badgePositions),
                        "topRight",
                        ""
                    )}
                />
            </Button>
        </div>
    </Story>
</Preview>

## Props

<Props of={Badge} />
