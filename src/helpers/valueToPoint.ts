export const valueToPx = (value: number): string => `${value.toFixed(2)}px`;

export const valueToEm = (value: number): string => `${value.toFixed(2)}em`;

export const valueToRem = (value: number): string => `${value.toFixed(2)}rem`;

export default valueToPx;
